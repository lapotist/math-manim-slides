# Publication Record: 2026-07-25

Status: public and verified.

## Repository

- URL: <https://github.com/lapotist/carlo-math-manim-slides>
- Initial public commit: `ebe43aa3a9bb60cbeb53f5164cb1fcf225d9c3e4`
- Visibility: public
- Default branch: `main`
- Description: `Intuitive Traditional Chinese math lessons built with Manim Slides`

The remote branch matched the local rewritten `main`, and the repository was
anonymously reachable with HTTP 200. The public root was checked for both
license files, `NOTICE.md`, `SOURCES.md`, `THIRD_PARTY.md`, the permission note,
catalog, lesson sources, QA attestations, tests, and locked build metadata.

## Release Boundary

The public repository contains project source, documentation, metadata, and
compact QA evidence. Ignored PDFs, source videos, rendered media, standalone
HTML, local environments, caches, credentials, and the private rewrite-backup
bundle were not pushed.

The path-based MIT and CC BY 4.0 split in `NOTICE.md` governs project-controlled
material. Cited source questions, PDFs, videos, branding, fonts, and dependency
assets remain excluded as documented there.

At publication, the honest catalog state remained 46 `visual_verified`, 20
`discovered`, 10 `blocked`, and 0 `published` lesson artifacts. Publishing the
source repository did not promote incomplete lessons or claim that all 76
cataloged problems have finished decks.

## Identity And Verification

Every reachable author and committer identity used the authenticated GitHub
account's ID-based no-reply address. The original local history was retained
only in a verified, ignored bundle under `build/backups/` and was not pushed.

Immediately before the initial push:

```text
pixi run test                  55 tests passed
pixi run validate-catalog      76 problems; 46 lessons; registries consistent
pixi run check-sources         source index current for 46 lessons
python compileall              passed
tracked source/media scan      no PDF, MP4, HTML, environment, or credential hit
```

The Carlo site owner asked to receive the finished repository URL. Sharing the
URL remains a maintainer follow-up and is not a condition of the content
license.
