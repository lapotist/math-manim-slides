# 第 4 題講者稿：梯形裡不變的分點

預估時間：8 分鐘

解題來源：正哥愛數學

題目定位：`112中一中資優班解析.pdf` 第 4 頁；公開解題影片
`K9kwq9apPR0`，00:00–01:48.80。

## 01 build_given_trapezoid｜先把題目真的畫出來

先不算比例。我們只把題目給的物件依序放回圖上。

`AB` 和 `CD` 是梯形的兩條平行底。下底 `AB` 有三份，上底 `CD` 只有一份，
所以 `AB=3CD`。

再畫對角線 `AC`。`E` 是它的中點，兩個短刻痕表示 `AE=EC`。

最後從 `B` 穿過 `E` 畫一直線。它與側邊 `AD` 相交的地方，就是題目中的同一個
點 `F`。

目前畫面只包含題目的已知條件，還沒有使用任何答案。 [PAUSE] [NEXT]

## 02 vary_shape_watch_f｜先用變形找感覺

現在讓梯形換幾個樣子。

第一個形狀比較高，也往右傾。注意每一次變形，我們都重新取 `AC` 的中點，
再重新畫 `BE`；`F` 永遠是兩條線真正的交點。

第二個形狀往左斜。`F` 跟著整條側邊移動，但它在 `AD` 上的相對位置，看起來
沒有亂跑。

最後停回一般位置。

這段運動只是觀察，不是證明。它提出的問題是：梯形的高度與傾斜改變時，
`F` 分割 `AD` 的比例會不會保持不變？ [PAUSE] [NEXT]

## 03 pause_on_unknown_split｜先停在真正的未知量

現在所有東西都停止移動。

題目要的是 `AF` 占整條 `AD` 的比例。畫面把 `AD` 與 `F` 突出，但還沒有
任何數字。

先猜一猜。這個比例需要知道梯形多高嗎？需要知道側邊傾斜多少嗎？ [PAUSE]

接下來，我們只加一個輔助點，看看已知的中點與平行線能不能一起發揮作用。
[NEXT]

## 04 extend_lines_to_g｜讓兩條原有直線相遇

先把上底 `CD` 所在的直線向左延長。

再把穿過 `B、E、F` 的直線繼續向左延長。兩條延長線相交的點叫做 `G`。

所以畫面上有兩排共線點：`G、D、C` 在同一直線上；`G、F、E、B` 也在同一
直線上。

這個新點的價值，不是多製造一條公式，而是讓 `E` 左右各出現一個可以直接
比較的三角形。 [PAUSE] [NEXT]

## 05 earn_equal_long_segment｜先證明兩個三角形一樣大

只看藍色的 `△GEC` 與綠色的 `△BEA`。

在 `E` 點，兩個角是對頂角，所以大小相等。

再看 `C` 與 `A` 的角。`GC` 和 `BA` 平行，而 `CA` 是同一條截線，所以這組
角也相等。

最後，中點條件已經告訴我們 `EC=EA`。

兩個角與它們之間的邊一一對應，因此 `△GEC` 與 `△BEA` 全等。

現在才能讀出對應邊：`GC=BA`。也就是說，新長出的整段 `GC`，恰好和原本的
三份下底一樣長。 [PAUSE] [NEXT]

## 06 split_three_units｜三份減一份

把 `CD` 當作一個長度單位 `u`。

因為 `AB=3CD`，所以 `AB=3u`。剛才又證明 `GC=AB`，因此整段 `GC` 也是
三份。

但 `GC` 已經包含了右側的 `CD`。從三份中扣掉這一份，左側延長段 `GD`
就剩兩份：

\[
GD=GC-CD=2u.
\]

所以畫面上方已經得到一個確定比例：

\[
GD:AB=2:3.
\]

下一步要把這個比例搬到題目真正關心的側邊。 [PAUSE] [NEXT]

## 07 earn_second_similarity｜F 周圍再出現一組相同的角

現在只看 `△GDF` 與 `△BAF`。

在 `F` 點，`GF` 與 `BF` 是同一直線，`DF` 與 `AF` 也是同一直線，所以這組
角是對頂角。

在 `D` 與 `A`，`GD` 平行於 `AB`，而 `DA` 是截線，所以第二組角也相等。

有兩組角相等，得到

\[
\triangle GDF\sim\triangle BAF.
\]

請注意對應順序：`G` 對 `B`，`D` 對 `A`，`F` 對 `F`。因此上方的
`GD` 對應 `AB`，側邊的 `DF` 對應 `AF`：

\[
\frac{GD}{AB}=\frac{DF}{AF}.
\]

比例現在有一條可靠的搬運路線。 [PAUSE] [NEXT]

## 08 transfer_two_to_three｜把上方比例搬到 AD

上方已經知道 `GD:AB=2:3`。

相似三角形告訴我們，同一個比例也出現在側邊：

\[
DF:AF=2:3.
\]

所以可以把 `DF` 寫成 `2k`，把 `AF` 寫成 `3k`。畫面用珊瑚色標兩份，用
黃色標三份。

先停在這裡。題目不是問 `DF:AF`，而是問 `AF` 占整條 `AD` 多少。分母還要
把兩段都算進去。 [PAUSE] [NEXT]

## 09 hold_before_fraction｜真正的答案前停點

把側邊攤成五個一樣大的格子。

前兩格是 `DF=2k`，後三格是 `AF=3k`。下方的大括號代表整條 `AD`。

現在只問一件事：三格，占全部幾格？ [PAUSE]

先在心裡完成。畫面還沒有顯示答案，也還沒有把整條長度寫成一個式子。
[NEXT]

## 10 reveal_three_fifths｜數完五份再揭示

整條 `AD` 是兩份加三份：

\[
AD=2k+3k=5k.
\]

分子 `AF` 是三份，分母 `AD` 是完整五份，所以

\[
\frac{AF}{AD}
=\frac{3k}{2k+3k}
=\frac35.
\]

答案是五分之三。

最後回到左側梯形。先前不論把梯形拉高、壓低或左右傾斜，只要兩條底仍是
三比一、`E` 仍是 `AC` 中點，同一個交點 `F` 就固定把 `AD` 放在這個比例上。

運動讓我們先發現不變；兩次三角形比較，才真正說明它為什麼不變。 [PAUSE]
