# Question 5 storyboard

## Teaching intent

這題表面上同時出現兩個函數、兩個正整數與一個參數。課程不從展開式開始，而先把條件畫成兩次「平衡」：同一個 `p` 必須讓輸入 `α` 和 `β` 都滿足左、右兩部機器輸出相同。接著把兩次相等合併成差值函數 `H_p(x)=f(x)-pg(x)` 的兩個零點，讓後面的代數始終有一個可見物件可以回去指認。

提出共同因子 `(x+1)` 時，畫面立刻回到數線檢查定義域。`x=-1` 的確是代數零點，但題目只收正整數，所以它不能算入 `α,β`。剩下的二次式被畫成穿過兩個正整數位置的拋物線；在此之後才用根的和與積讀出

\[
\alpha+\beta=p,
\qquad
\alpha\beta=\frac{6p+45}{5}.
\]

用第一式取代第二式中的 `p`，把三個未知量縮成兩個正整數。因式分解不以「突然看出來」帶過：先把 `5αβ-6α-6β=45` 乘以 5，再用一個 `5α` 乘 `5β` 的長方形顯示兩條寬 6 的帶狀區域。兩條帶子的角落被重複扣除，因此補回 `6\cdot6=36`，畫面才壓縮成

\[
(5\alpha-6)(5\beta-6)=261.
\]

列正因數對以前，先處理容易漏掉的符號邊界：正整數 `n` 只保證 `5n-6\ge -1`；若乘積是正的 261，兩因數必須同號，而唯一可能的負值只有 `-1`，不可能和另一個至少為 `-1` 的數相乘得到 261。因此兩因數確實都為正。最後列完 261 的三組正因數對，再用兩因數都必須除以 5 餘 4 的條件篩選，而不是猜中 `9,29` 就停止。

`decode_surviving_pair` 解回 `α=3,β=7`，`hold_parameter_sum` 停在 `p=3+7`，讓觀眾自己完成最後一步。`reveal_parameter` 才揭示 `p=10`，再由 `verify_parameter_in_original_equations` 以原函數數值檢查「存在」與「恰有兩個」。

## Source reconstruction

- 典藏頁面：`https://sites.google.com/chjs.ntpc.edu.tw/carlovemath/%E4%B8%AD%E4%B8%80%E4%B8%AD%E8%B3%87%E5%84%AA%E7%8F%AD/112%E4%B8%AD%E4%B8%80%E4%B8%AD%E8%B3%87`。
- 凍結頁面 SHA-256：`8ef9b13e28c992e21dd874cfeabbb6ad66dcacdeec3c326f058ff561de9ba094`。
- 題目資產：`112中一中資優班解析.pdf`，Drive ID `1wZiSE5cZZI9Fr_YJovGP_H3aWoFXxc_H`，catalog 存取狀態 `public`，SHA-256 `277e49efb3aca44666e17c2c4135b6047922f25adee58952560416996140acbd`，共 17 頁，本題在第 5 頁。
- PDF 第 5 頁的完整題意是：「已知 `f(x)=(x+1)(5x^2+45)`，`g(x)=(x+1)(5x-6)`，若恰有兩個正整數 `α,β`，滿足 `f(α)=p\cdot g(α)`，`f(β)=p\cdot g(β)`，則 `p` 的所有可能值的總和為____。」該頁 `解析` 區域沒有書寫內容，所以 PDF 只能定位題目，不能單獨作為解題證據。
- 解題資產：正哥愛數學新站公開影片 `https://www.youtube.com/watch?v=dMXJ-FbZKxU`，provider ID `dMXJ-FbZKxU`，catalog 存取狀態 `public_stream`，驗證標題 `112中ㄧ中資優班填充5`。
- 本地研究副本長 223.283084 秒、解析度 518x360、30 fps，SHA-256 `f24eac32bfad2da0bc312805850972b6bf55400a62b4751f0935be0fe90711a8`。此 SHA 只描述研究時保留的特定串流檔。
- 影片全段都在處理第 5 題。可見推理令差值為零，提出 `(x+1)`，對剩餘二次式使用根與係數關係，得到 `(5α-6)(5β-6)=261`，選取 `9,29`，解出 `α=3,β=7`，最後得到 `p=10`。
- 00:03:42.525 仍是穩定、無遮擋的完整解答；約 00:03:42.550 開始出現裝置介面遮罩。完整定位保守記為 `00:00-03:42.52`。
- 來源解法和獨立重建的方程、因數對與答案一致。來源畫面沒有把補 36 畫成面積，也沒有完整展示負因數排除、三組正因數對的窮舉與回代原函數；本課以原創視覺補足這些論證。
- 專案原創課程內容採 CC BY 4.0，程式碼採 MIT；來源素材不納入本專案授權。

## Independent mathematics check

令

\[
H_p(x)=f(x)-p\,g(x).
\]

由題目給定的函數，

\[
H_p(x)
=(x+1)\bigl[5x^2+45-p(5x-6)\bigr]
=(x+1)Q_p(x),
\]

其中

\[
Q_p(x)=5x^2-5px+(6p+45).
\]

因為題目只考慮正整數 `x`，所以 `x+1\ne0`。因此符合原式的正整數恰好是 `Q_p(x)=0` 的正整數根。若這兩個不同的根為 `α<β`，韋達定理給出

\[
\alpha+\beta=p,
\qquad
\alpha\beta=\frac{6p+45}{5}.
\]

消去 `p`：

\[
5\alpha\beta=6(\alpha+\beta)+45,
\]

所以

\[
5\alpha\beta-6\alpha-6\beta=45.
\]

兩邊乘以 5，再加 36：

\[
25\alpha\beta-30\alpha-30\beta+36=261,
\]

即

\[
(5\alpha-6)(5\beta-6)=261.
\]

現在仔細檢查符號。`α,β` 為正整數，所以每個因數至少是 `-1`。乘積 261 為正，兩因數必須同為正或同為負；但它們若為負，只可能都是 `-1`，乘積會是 1，不會是 261。因此兩因數都為正。

因為

\[
261=3^2\cdot29,
\]

其正因數對完整列表是

\[
(1,261),\quad(3,87),\quad(9,29).
\]

任何形如 `5n-6` 的整數都除以 5 餘 4。前兩組的餘數分別是 `(1,1)` 與 `(3,2)`，只有 `(9,29)` 的餘數為 `(4,4)`。由 `α<β`，因此

\[
5\alpha-6=9,
\qquad
5\beta-6=29,
\]

得到 `α=3,β=7`，進而

\[
p=\alpha+\beta=10.
\]

還要檢查反向是否成立。代入 `p=10`：

\[
Q_{10}(x)=5x^2-50x+105=5(x-3)(x-7).
\]

所以正整數根恰為 3、7；完整差值另有共同因子根 `x=-1`，但它不在正整數範圍。直接代回原函數亦得

\[
f(3)=360=10g(3),
\qquad
f(7)=2320=10g(7).
\]

因此 `p=10` 不只必要，也確實產生恰好兩個正整數解；所有可能值的總和就是 10。

`deck.py` 在匯入時以精確整數檢查 261 的完整正因數對、模 5 篩選、解碼結果、韋達關係、`p=10` 的正整數根集合，以及兩組原函數數值。斷言和動畫物件分開建立。

## Visual grammar

- 黃色固定代表 `α`，綠色固定代表 `β` 或通過篩選的區域；兩者從最初的輸入卡一路延續到根、根和根積、解碼與最後核對。
- 藍色代表原函數或主要二次式；紫色代表含參數 `p` 的另一側；珊瑚色代表不合定義域或篩選失敗的項目。
- 先畫兩次平衡，再引入差值函數。`H_p` 是對可見關係的命名，不是沒有來由的開場定義。
- `x=-1` 先在數線上出現，再被交叉排除。共同因子不可因為「顯然不合」而只在講稿中略過。
- 根的和用相接長條表示，根的積用面積表示；右側係數比較只翻譯左側已存在的物件。
- 補 36 的長方形動畫依序顯示整體、兩條扣除帶、重疊角落與剩餘區域，公式隨圖形逐步出現。
- 因數對列表必須完整保留三行。前兩行明確標示不合，`9,29` 只有在模 5 條件出現後才被框選。
- `hold_parameter_sum` 是答案前停點：畫面可以顯示 `α=3`、`β=7`、`p=3+7`，但標題、畫面與講稿都不出現 `10`。

## Beat map

### 01 show_two_integer_tests

先不要急著展開式子。我們把題目的條件看成兩次輸入測試。

動作目的：讓「先顯示兩個整數輸入測試」在穩定畫面上單獨成立，再進入下一步。

### 02 meet_two_integer_hits

綠色的 `β` 也要通過同樣的測試。最重要的是，兩行右邊用的是同一個 `p`，而且 `α`、`β` 是兩個不同的正整數。

動作目的：讓「強調兩次測試共用同一個 p」在穩定畫面上單獨成立，再進入下一步。

### 03 turn_matches_into_zeros

相等的兩邊相減會得到零。於是定義一個差值：

動作目的：讓「把兩次相等各自改寫成零」在穩定畫面上單獨成立，再進入下一步。

### 04 place_two_positive_roots

數線上的兩個點，就是同一個差值函數的兩個零點。這一步的好處是，我們不再追兩條分開的等式，而是問：哪一個式子會有兩個不同的正整數零點？

動作目的：讓「把兩個零放到同一條數線」在穩定畫面上單獨成立，再進入下一步。

### 05 factor_common_root

代入後，兩個函數都有共同因子 `x+1`，所以差值可以寫成畫面上這個乘積。

動作目的：讓「提出共同因子並定位負根」在穩定畫面上單獨成立，再進入下一步。

### 06 discard_common_root

這個定義域檢查很重要。提出共同因子之後，不能把它產生的所有根都直接算進題目。

動作目的：讓「排除負根並保留二次式」在穩定畫面上單獨成立，再進入下一步。

### 07 see_remaining_quadratic

現在想像剩下的二次函數圖形。它必須穿過水平軸上的兩個不同位置；這兩個位置就是正整數 `α` 和 `β`。

動作目的：讓「先看穿過兩個正根的拋物線」在穩定畫面上單獨成立，再進入下一步。

### 08 write_two_quadratic_forms

第二行從零點來讀：

動作目的：讓「並列係數式與根式」在穩定畫面上單獨成立，再進入下一步。

### 09 picture_root_sum_and_product

左上兩段長條接在一起，代表根的和 `α+β`。左下長方形的兩個邊長是 `α`、`β`，所以面積代表根的積 `αβ`。

動作目的：讓「先把根和根積畫成長度與面積」在穩定畫面上單獨成立，再進入下一步。

### 10 read_sum_and_product

把根式展開：

動作目的：讓「再從係數讀出兩條關係」在穩定畫面上單獨成立，再進入下一步。

### 11 substitute_root_sum_for_p

畫面上的替換很直接：凡是看到 `p`，都可以換成 `α+β`。

動作目的：讓「用根的和取代參數 p」在穩定畫面上單獨成立，再進入下一步。

### 12 remove_parameter_p

把含 `α`、`β` 的項移到左邊：

動作目的：讓「整理成只含兩個整數的式子」在穩定畫面上單獨成立，再進入下一步。

### 13 subtract_rectangle_strips

先把上一行乘以 5：

動作目的：讓「從大長方形扣掉兩條寬六的帶」在穩定畫面上單獨成立，再進入下一步。

### 14 restore_overlap_corner

減去 `30β`，就是從一側拿掉寬 6 的直條；減去 `30α`，就是再從另一側拿掉高 6 的橫條。

動作目的：讓「補回重複扣除的角落」在穩定畫面上單獨成立，再進入下一步。

### 15 complete_factor_rectangle

現在可以用整數因數來找 `α`、`β`，但列正因數前還有一個符號邊界要確認。

動作目的：讓「把面積關係壓成因數乘積」在穩定畫面上單獨成立，再進入下一步。

### 16 close_negative_factor_case

因為 `α`、`β` 是正整數，`5α-6` 和 `5β-6` 都至少是 `-1`。

動作目的：讓「先證明兩個因數都為正」在穩定畫面上單獨成立，再進入下一步。

### 17 test_first_factor_pair

分解 `261=3^2\cdot29`，所有正因數對只有三組：`1\times261`、`3\times87`、`9\times29`。

動作目的：讓「建立餘數門檻並排除第一組」在穩定畫面上單獨成立，再進入下一步。

### 18 test_second_factor_pair

第二組 `3\times87` 的餘數是 3、2，也不合。

動作目的：讓「檢查並排除第二組」在穩定畫面上單獨成立，再進入下一步。

### 19 accept_third_factor_pair

第三組 `9\times29` 的餘數恰好是 4、4。完整檢查後，只有這一組能留下來。

動作目的：讓「確認第三組唯一通過」在穩定畫面上單獨成立，再進入下一步。

### 20 decode_surviving_pair

因為先約定 `α<β`，較小的因數 9 對應 `α`：

動作目的：讓「把九與二十九解回兩個根」在穩定畫面上單獨成立，再進入下一步。

### 21 hold_parameter_sum

現在回到根的和：`p=α+β`。畫面已經把它寫成 `p=3+7`。

動作目的：讓「回到根的和並停在最後相加」在穩定畫面上單獨成立，再進入下一步。

### 22 reveal_parameter

最後得到

動作目的：讓「揭示 p 並核對正整數根數」在穩定畫面上單獨成立，再進入下一步。

### 23 verify_parameter_in_original_equations

三個零點是 `-1`、3、7，其中只有 3、7 是正整數，所以正整數零點恰好兩個。

動作目的：讓「逐一回代兩個原始等式」在穩定畫面上單獨成立，再進入下一步。

## Build constraints

- 只編輯 `lessons/tcfs_112_math_gifted/q05/`；媒體寫入專屬 ignored build 目錄。
- 場景類別為 `CarloTcfs112MathQ05`，共 23 個 beat，全部 `loop=false`。
- 中文只使用 `label()`；`MathTex` 只放 ASCII 數學內容。
- 必須先做低畫質 smoke，再以 `--quality h` 隔離渲染。
- 必須檢查 23 個 endpoint、完整每秒 sweep，以及平衡轉零點、`x=-1` 排除、根圖、補 36、因數對篩選、答案前停點和答案揭示的中間影格。
- lesson worker 停在 `draft_rendered`，不執行 `freeze-qa`，也不修改 collection state。
