# Question 8 storyboard

## Teaching intent

這是一個教師帶領的平均數 worked example。預備知識是「總和＝個數×平均」、分數四則運算，以及從 `1` 加到 `25` 的配對求和。學生最可能先產生的困惑是：「同一顆 15 號球從 A 拿走、放進 B，怎麼可能讓兩邊平均都上升？」另一個常見錯誤是算平均變化時沿用移動前的分母；A 移動後是 `m-1` 顆，B 移動後是 `26-m` 顆。

課程先讓同一顆黃色 15 號球保持身分，從 A 籃移到 B 籃。移動後才同時升起兩支平均尺。這個具體事件先回答方向：若拿走 15 使 A 的平均上升，15 必低於 A 的原平均；若加入 15 使 B 的平均上升，15 必高於 B 的原平均。因此畫面建立

\[
x_B<15<x_A.
\]

接著不先寫兩個完整總和式，而用「每份上升 `1/4`」的綠色小磚追蹤差額。A 拿走 15 後有 `m-1` 份，所以 15 到 `x_A` 的缺口必須支付

\[
x_A-15=\frac{m-1}{4}.
\]

B 加入 15 後有 `26-m` 份，所以 15 超過 `x_B` 的部分必須支付

\[
15-x_B=\frac{26-m}{4}.
\]

兩式分別重建原平均，並讓學生在同一條尺上看見 `x_A-x_B=25/4` 與 `m` 無關。這個固定距離稍後成為簡化總和式的理由。

總號碼 325 也不直接宣布。畫面先配對 `1+25`、`2+24`、一直到 `12+14`，再補中間的 13，才得到 `12\cdot26+13=325`。兩籃重新合併後，先顯示來源所用的總和關係

\[
m x_A+(25-m)x_B=325.
\]

然後以一條 25 份的 `x_B` 基線和 A 籃的 `m` 份額外高度，把同一式重新排列成

\[
325=25x_B+m(x_A-x_B).
\]

這個畫面讓二次項的消失不是神祕的展開抵消，而是「所有球先鋪 B 的平均，A 的 m 顆再補平均差」。代入前面已經看見的平均與差距，方程縮到 `m=18/2`。`hold_last_division` 在此穩定停住，不出現答案；`reveal_count` 才揭示 `m=9`。

最後不能只停在必要條件。`build_candidate_partition` 構造

\[
A=\{13,14,15,16,17,18,19,20,21\},
\]

其餘球放入 B。黃色 15 再次從 A 移到 B，畫面直接核對兩邊平均都恰好增加 `1/4`。最後 realization 是：看似矛盾的雙升並不矛盾；15 正好夾在兩個原平均之間，而它在兩邊造成的差額共同鎖定了籃子個數。

## Source reconstruction

- 典藏頁面：`https://sites.google.com/chjs.ntpc.edu.tw/carlovemath/%E4%B8%AD%E4%B8%80%E4%B8%AD%E8%B3%87%E5%84%AA%E7%8F%AD/112%E4%B8%AD%E4%B8%80%E4%B8%AD%E8%B3%87`。
- 凍結頁面 SHA-256：`8ef9b13e28c992e21dd874cfeabbb6ad66dcacdeec3c326f058ff561de9ba094`。
- 題目資產：`112中一中資優班解析.pdf`，Drive ID `1wZiSE5cZZI9Fr_YJovGP_H3aWoFXxc_H`，catalog 存取狀態 `public`，SHA-256 `277e49efb3aca44666e17c2c4135b6047922f25adee58952560416996140acbd`，共 17 頁，本題在第 8 頁。
- PDF 第 8 頁的實際題意是：編號 `1` 到 `25` 的 25 顆足球分配在籃子 A、B；15 號球原在 A。把 15 號球由 A 移到 B 後，兩籃號碼平均都比原平均增加 `1/4`；求 A 最初的球數。該頁 `解析` 區域空白，所以 PDF 只能定位題目，不能單獨驗證解法。
- 解題資產：正哥愛數學新站公開影片 `https://www.youtube.com/watch?v=yxlLBTcz4kg`，provider ID `yxlLBTcz4kg`，catalog 存取狀態 `public_stream`，驗證標題 `112中ㄧ中資優班填充8`。
- 本地研究串流長 284.537324 秒、解析度 518x360、30 fps，SHA-256 `23431c7e739e7ee55631ee29143d8ee6a0d16f4083041a28f0977eac31570dcf`。
- 影片全段都在處理第 8 題。來源令 A 原有 `m` 顆、平均 `x_1`，B 原有 `25-m` 顆、平均 `x_2`。可見推理寫出 `mx_1-15=(m-1)(x_1+1/4)` 與 `(25-m)x_2+15=(26-m)(x_2+1/4)`，導出 `x_1=(m+59)/4`、`x_2=(m+34)/4`，再用 `mx_1+(25-m)x_2=325` 得 `m=9`。
- `00:04:43.80` 仍是完整、清楚、無遮擋的最後答案；`00:04:43.85` 已開始模糊並出現裝置控制介面。因此精確定位記為 `00:00-04:43.80`。
- 來源結論與獨立重建一致。來源沒有展示一組實際可行分籃，也沒有把兩個差額畫成重複的 `1/4` 份額；本課新增這些原創檢查與視覺，並以共同 `x_B` 基線解釋來源總和式中的抵消。
- 專案原創課程內容採 CC BY 4.0，程式碼採 MIT；來源素材不納入本專案授權。

## Independent mathematics check

設 A 原有 `m` 顆，原平均為 `x_A`；B 原有 `25-m` 顆，原平均為 `x_B`。移動後 A 仍需非空，且 B 原本也應非空，因此

\[
2\le m\le24.
\]

A 拿走 15 號球後，球數變為 `m-1`，平均變為 `x_A+1/4`：

\[
m x_A-15=(m-1)\left(x_A+\frac14\right).
\]

消去兩邊共同的 `(m-1)x_A`，得到

\[
x_A-15=\frac{m-1}{4},
\qquad
x_A=\frac{m+59}{4}.
\]

B 加入 15 號球後，球數變為 `26-m`，平均變為 `x_B+1/4`：

\[
(25-m)x_B+15=(26-m)\left(x_B+\frac14\right).
\]

因此

\[
15-x_B=\frac{26-m}{4},
\qquad
x_B=\frac{m+34}{4}.
\]

兩個原平均的差固定為

\[
x_A-x_B=\frac{25}{4}.
\]

所有標號的總和為

\[
1+2+\cdots+25=\frac{25\cdot26}{2}=325.
\]

按兩籃重組：

\[
m x_A+(25-m)x_B=325.
\]

以 `x_B` 為共同基線：

\[
325=25x_B+m(x_A-x_B).
\]

代入 `x_B=(m+34)/4` 與 `x_A-x_B=25/4`：

\[
325=25\cdot\frac{m+34}{4}+m\cdot\frac{25}{4}.
\]

除以 25 並化簡：

\[
13=\frac{m+34}{4}+\frac m4,
\qquad
52=2m+34,
\qquad
m=9.
\]

這個一次方程在實數中已只有一解，所以在整數範圍 `2<=m<=24` 也唯一。但仍須證明題目條件確實能實現。取

\[
A=\{13,14,15,16,17,18,19,20,21\},
\]

其餘 `1` 到 `25` 的球全放在 B。A 原有 9 顆、總和 153、平均 17；B 原有 16 顆、總和 172、平均 `43/4`。移動 15 後：

\[
\frac{153-15}{8}=\frac{138}{8}=\frac{69}{4}=17+\frac14,
\]

\[
\frac{172+15}{17}=\frac{187}{17}=11=\frac{43}{4}+\frac14.
\]

兩邊確實各上升 `1/4`，所以 `m=9` 同時是必要且可行的唯一答案。

`deck.py` 在匯入時以 `Fraction` 枚舉 `2<=m<=24` 的所有聚合方程候選、確認只剩 9，並精確檢查顯式分籃的聯集、互斥性、球數、總和與四個移動前後平均。這些斷言不依賴動畫座標。

## Visual grammar

- A 籃與 `x_A` 固定用藍色；B 籃與 `x_B` 固定用紫色。
- 同一顆 15 號球固定用黃色，從開場、第一次移動、差額尺到最後驗證都保留身分。
- 每一份平均上升 `1/4`、兩平均的固定差與合法補量固定用綠色。
- 325 和「必要但還未證明可行」的問題使用珊瑚色；最終答案回到黃色。
- 平均尺只表達順序與差額，不把未知平均放在虛構刻度上。第 6 段才把 `x_B<15<x_A` 合到同一條尺。
- 四分之一小磚只顯示三塊與省略號，括號明確標示總共有 `m-1` 或 `26-m` 份，避免把示意塊數誤當實際球數。
- 第 9 段的長條不是個別球值分布；它是總和的平均基線分解。畫面標示 `25x_B` 與 `m(x_A-x_B)`，講稿同步說明其聚合意義。
- 第 10 段是真正的答案前停點：標題、畫面與講稿只到 `m=18/2`，不出現 9。
- 第 12 段重新使用黃色 15 號球完成一次實際移動，讓最終核對回到開場物件，而不是只留在代數式。

## Beat map

### 01 build_two_baskets

我們有 25 顆球，編號從 1 到 25，全部分在 A、B 兩個籃子裡。

動作目的：讓「先建立兩個籃子」在穩定畫面上單獨成立，再進入下一步。

### 02 meet_two_baskets

先把 A 原本的球數叫做 `m`。那麼 B 原本就有 `25-m` 顆。

動作目的：讓「再放入球數與十五號球」在穩定畫面上單獨成立，再進入下一步。

### 03 move_ball_fifteen

黃色的 15 離開 A，進入 B。

動作目的：讓「把十五號球從 A 移到 B」在穩定畫面上單獨成立，再進入下一步。

### 04 show_new_mean_gauges

球落定後，題目告訴我們一件很特別的事：A 的平均從 `x_A` 上升 `1/4`，B 的平均也從 `x_B` 上升 `1/4`。

動作目的：讓「移動後再顯示兩個新平均」在穩定畫面上單獨成立，再進入下一步。

### 05 measure_a_deficit

豎尺下面的黃色點是 15，上面的藍色點是 A 的原平均 `x_A`。

動作目的：讓「先量十五與 A 新平均的差」在穩定畫面上單獨成立，再進入下一步。

### 06 balance_a_mean_change

拿走一顆後，A 還有 `m-1` 顆。平均每顆上升 `1/4`，所以總共要補出 `m-1` 份四分之一。

動作目的：讓「用四分之一補回 A 的平均變化」在穩定畫面上單獨成立，再進入下一步。

### 07 derive_a_mean

左邊長條的黃色部分是 15，綠色部分是剛才看見的缺口 `(m-1)/4`。

動作目的：讓「把 A 的平衡關係寫成方程」在穩定畫面上單獨成立，再進入下一步。

### 08 solve_a_mean

這個式子也保留了剛才的方向感：A 的原平均確實在 15 上方。

動作目的：讓「讀出 A 原平均」在穩定畫面上單獨成立，再進入下一步。

### 09 measure_b_surplus

這次紫色點 `x_B` 在下，黃色的 15 在上。

動作目的：讓「先量十五高於 B 新平均多少」在穩定畫面上單獨成立，再進入下一步。

### 10 balance_b_mean_change

加入之後，B 一共有 `26-m` 顆。每一份平均提高 `1/4`，所以 15 高出的部分正好分成 `26-m` 份：

動作目的：讓「用四分之一對回 B 的平均變化」在穩定畫面上單獨成立，再進入下一步。

### 11 place_old_means_on_axis

先把兩個舊平均放在同一條尺上：`x_B` 在 15 下方，`x_A` 在 15 上方。下一張再把距離式整理並比較。

動作目的：讓「把兩個舊平均放在十五兩側」在穩定畫面上單獨成立，再進入下一步。

### 12 compare_old_means

從剛才的距離式，把缺口移到 15 的另一側：

動作目的：讓「算出兩個舊平均的距離」在穩定畫面上單獨成立，再進入下一步。

### 13 pair_numbers_around_fifteen

把最小和最大的號碼配在一起：`1+25=26`。

動作目的：讓「先把數字成對配在十五兩側」在穩定畫面上單獨成立，再進入下一步。

### 14 earn_total_325

無論球怎麼分，A、B 兩籃的號碼總和加起來永遠是 325。

動作目的：讓「再加總十二對與中央十三」在穩定畫面上單獨成立，再進入下一步。

### 15 reassemble_basket_totals

A 原本有 `m` 顆，平均是 `x_A`，所以 A 的號碼總和是 `m x_A`。

動作目的：讓「把兩籃總和分別寫回框中」在穩定畫面上單獨成立，再進入下一步。

### 16 merge_basket_totals

兩支箭頭把兩籃重新合併，必須得到剛才算出的 325：

動作目的：讓「合併後得到總數方程」在穩定畫面上單獨成立，再進入下一步。

### 17 build_b_baseline

先假想 25 顆球的平均基線全都放在 `x_B`。紫色長條的總量就是 `25x_B`。

動作目的：讓「先把兩籃都改看成 B 的基準」在穩定畫面上單獨成立，再進入下一步。

### 18 regroup_on_b_baseline

而前面已經知道平均差是 `25/4`，所以

動作目的：讓「把多出的差額寫成方程」在穩定畫面上單獨成立，再進入下一步。

### 19 derive_last_division

把整式除以 25，並代入

動作目的：讓「逐行整理到最後一次除法」在穩定畫面上單獨成立，再進入下一步。

### 20 hold_last_division

移去 34，就有 `18=2m`，也就是畫面上的

動作目的：讓「停在最後商數揭示前」在穩定畫面上單獨成立，再進入下一步。

### 21 reveal_count

最後得到

動作目的：讓「揭示唯一的原始球數」在穩定畫面上單獨成立，再進入下一步。

### 22 build_candidate_partition

現在給一組真的分法。A 放入從 13 到 21 的九顆球；黃色的 15 確實在 A。B 放入剩下的 `1` 到 `12`，以及 `22` 到 `25`。

動作目的：讓「先照候選數量重建兩籃」在穩定畫面上單獨成立，再進入下一步。

### 23 verify_real_partition

現在讓黃色的 15 再走一次。A 剩下的總和是 138、共有 8 顆，新平均為

動作目的：讓「實際移球並核對兩個新平均」在穩定畫面上單獨成立，再進入下一步。

## Build constraints

- 只編輯 `lessons/tcfs_112_math_gifted/q08/`；媒體寫入專屬 ignored build 目錄。
- 場景類別為 `CarloTcfs112MathQ08`，共 23 個 beat，全部 `loop=false`。
- 中文只使用 `label()`；`MathTex` 只放 ASCII 數學內容。
- 必須先做低畫質 smoke，再以 `--quality h` 隔離渲染。
- 必須檢查 23 個 endpoint、完整每秒 sweep，以及兩次球移動、平均尺上升、A/B 四分之一磚、基線重組、答案前停點與最後平均核對的中間影格。
- lesson worker 停在 `draft_rendered`，不執行 `freeze-qa`，也不修改 collection state。
