# ROC 112 Fill-in Question 10 storyboard

## Teaching intent

這題同時問周長與面積，但兩個量看的是不同東西。課程不先展示 112 個圖形的
完成公式，而是讓同一個正三角形真的向右移動半個邊長：先建立 `n=1`，再把
第二個放到 `n=2`，最後重複同一個動作得到 `n=3`。只有三個三角形都 settled
之後，才把原來每一條邊淡化並描出聯集外輪廓。觀眾先分清楚「原圖的所有邊」
與「真正要量的外輪廓」，才進入周長。

周長分成底邊與斜邊。從 `n=2` 加到 `n=3` 時，舊的最後一條長度 1 的外斜邊
被三段 `1/2、1/2、1` 取代，因此斜邊淨增 1；底邊向右多半格，淨增 `1/2`。
下一張不只靠遞迴猜測，而是直接數一般 `n` 的所有外露段：左右兩條完整外邊，
以及相鄰頂點間的 `2(n-1)` 段半邊長，斜邊總長 `n+1`；底邊從第一個長度 1
開始，每新增一個多 `1/2`，總長 `(n+1)/2`。兩者相加得到一般周長。

面積重新從一個單位正三角形開始。先畫高，從直角三角形得到
`h=sqrt(3)/2`，所以單一面積是 `sqrt(3)/4`。再把相鄰兩個三角形的交集填色，
看見它是邊長 `1/2` 的正三角形，面積縮為原來的 `1/4`，即
`sqrt(3)/16`。第三個三角形出現時，特別標出第一個與第三個只共用一個點；
更遠的三角形完全分離。因而任何三重交集都沒有正面積，容斥只需扣掉
`n-1` 個相鄰交集。

一般化之前，畫面先逐一核對 `n=1,2,3` 的聯集：以 `sqrt(3)/16` 為共同單位，
面積係數是 `4、7、10`。這既讓觀眾看見每次淨增 3，也與精確座標多邊形的
鞋帶公式核對一致。第 10 beat 才把 `n=112` 同時代入兩個一般式，但兩張卡都
保留問號，講者也不說任何答案。第 11 beat 沿外輪廓走完才顯示周長；第 12
beat 把聯集填滿才顯示面積。完整數對直到第 13 beat 回到同一個「外框加填色」
圖後才出現。

預估 11 分鐘。十三個 beat 全部 `loop=false`，每個停點都保持靜止可朗讀。

## Source and provenance

- 典藏頁面：`https://sites.google.com/chjs.ntpc.edu.tw/carlovemath/%E4%B8%AD%E4%B8%80%E4%B8%AD%E8%B3%87%E5%84%AA%E7%8F%AD/112%E4%B8%AD%E4%B8%80%E4%B8%AD%E8%B3%87`。
- 凍結頁面 SHA-256：`8ef9b13e28c992e21dd874cfeabbb6ad66dcacdeec3c326f058ff561de9ba094`。
- 題目資產：`112中一中資優班解析.pdf`，Drive ID
  `1wZiSE5cZZI9Fr_YJovGP_H3aWoFXxc_H`，SHA-256
  `277e49efb3aca44666e17c2c4135b6047922f25adee58952560416996140acbd`。
  第 10 頁的題目與圖二、圖三、圖四是點陣內容，文字層只有題號來源；本課以
  實際頁面影像核對題意與排列。
- 解題資產：正哥愛數學新站公開影片
  `https://www.youtube.com/watch?v=W9yrKSMoY-0`，provider ID
  `youtube:W9yrKSMoY-0`，驗證標題 `112中ㄧ中資優班填充10`，catalog 存取狀態
  `public_stream`。
- 本次逐片檢查使用 518x360、長 219.800091 秒的研究副本，SHA-256
  `a3454d70b9ad45225cacbb1a27c71ec36f7b06711a644a5fea00b5f31bcf1a8d`。
  完整解題畫面在 `03:39.10` 仍無遮擋；`03:39.15` 已出現裝置覆蓋，因此保守
  定位為 `00:00-03:39.10`。
- 來源影片把單位邊分成半邊長，為外露斜邊與底邊著色，算得周長
  `169.5=339/2`；面積則把一個單位正三角形分成四個邊長 `1/2` 的小正三角形，
  以 `4+3*111` 個小面積單位得到 `337sqrt(3)/16`。本課保留這兩個結果與核心
  分割觀察，但以獨立座標、外輪廓證明、容斥與小例精確多邊形核對重建論證。
- 課程不使用 PDF 或影片畫面、手寫字、聲音、標誌、配色或原句。所有圖形、
  動畫、文字與程式都是本專案重建。權利狀態維持 `pending_cc0_scope`；公開可看
  不等於已確認 CC0 再授權範圍。

## Exact problem reconstruction

題目先把 3 個邊長為 1 的正三角形按圖二方式擺放，描出聯集輪廓成圖三；再以
同一規則放 4 個，描成圖四。依此規則放 112 個三角形並描其輪廓形成圖形 `A`，
若 `A` 的周長為 `r`、面積為 `s`，求數對 `(r,s)`。

題目點陣圖與來源影片的半邊標記共同確認：每一個新三角形與前一個同方向、
同高度，整體水平向右平移 `1/2`，不是沿斜邊旋轉，也不是左右交替翻轉。以第一
個三角形左下角為原點，第 `i` 個三角形可精確寫成

\[
T_i=\operatorname{conv}\left\{
\left(\frac i2,0\right),
\left(\frac i2+1,0\right),
\left(\frac i2+\frac12,\frac{\sqrt3}{2}\right)
\right\},\qquad i=0,\ldots,n-1.
\]

這個座標模型直接產生題目中的連續頂峰、半高谷點與完整底邊。

## Boundary and domain audit

- `n` 是正整數；一般式在 `n=1` 也成立，此時沒有相鄰交集。
- 聯集的左、右外斜邊各長 1。每一對相鄰頂點之間有兩段長 `1/2` 的邊界，
  共 `2(n-1)` 段。因此斜邊總長 `2+(n-1)=n+1`。
- 底邊從 `x=0` 延伸到 `x=(n+1)/2`，長 `(n+1)/2`。它是一條連續外邊，不能
  把各三角形底邊全部相加，因為內部重疊的底邊不在聯集外輪廓上。
- 相鄰交集 `T_i intersect T_(i+1)` 的頂點是
  `(i/2+1/2,0)`、`(i/2+1,0)`、`(i/2+3/4,sqrt(3)/4)`，是邊長 `1/2`
  的正三角形。
- `T_i` 與 `T_(i+2)` 只在 `(i/2+1,0)` 相交，交集面積為 0；索引差更大時
  不相交。任取三個不同索引，最小與最大至少差 2，所以三重交集面積必為 0。
- 邊界上的交點雖屬於多個三角形，面積為 0，不影響容斥的面積數值；但它仍是
  多邊形邊界頂點，不能從輪廓追蹤中任意省略。

## Independent mathematics check

聯集的斜邊與底邊分別為

\[
L_{\mathrm{slant}}=2\cdot1+2(n-1)\cdot\frac12=n+1,
\qquad
L_{\mathrm{bottom}}=1+(n-1)\cdot\frac12=\frac{n+1}{2}.
\]

所以

\[
r_n=\frac{3(n+1)}2,
\qquad
r_{112}=\frac{3\cdot113}{2}=\frac{339}{2}.
\]

單一正三角形面積是 `sqrt(3)/4`，每一個相鄰交集面積是 `sqrt(3)/16`。
只有 `n-1` 組相鄰交集有正面積，且沒有正面積三重交集，因此

\[
s_n=n\frac{\sqrt3}{4}-(n-1)\frac{\sqrt3}{16}
=\frac{(3n+1)\sqrt3}{16},
\]

\[
s_{112}=\frac{337\sqrt3}{16}.
\]

獨立的精確多邊形檢查不使用動畫文字。`deck.py` 以 `Fraction` 表示 `x` 與
`y/sqrt(3)`，用凸多邊形裁切計算交集，再以鞋帶公式與逐邊距離算得：

| `n` | 精確周長 | 精確面積 |
| ---: | ---: | ---: |
| 1 | `3` | `sqrt(3)/4` |
| 2 | `9/2` | `7sqrt(3)/16` |
| 3 | `6` | `5sqrt(3)/8` |

程式另外核對前五個三角形的每一組兩兩交集，以及前六個三角形的每一組三重
交集；所有非相鄰與三重交集都沒有正面積。最後結果為

\[
(r,s)=\left(\frac{339}{2},\frac{337\sqrt3}{16}\right).
\]

## Visual grammar

- 藍色是固定的單位正三角形；黃色是正在向右移動的下一個三角形與半格位移。
- 珊瑚色只代表外輪廓、斜邊周長與 `r`；綠色只代表聯集填色、面積與 `s`。
- 紫色只代表相鄰重疊，讓扣除項在圖與公式中保持同一角色。
- 灰色是已經理解、暫時退到背景的原始三角形邊。原始邊不會被誤畫成答案輪廓。
- 攝影機固定。`n=112` 只用同規則的短示意條帶，不把 112 個小三角形壓成
  無法閱讀的裝飾紋理。
- 第 10 beat 兩張結果卡都顯示問號；`339/2` 第一次出現在第 11 beat，
  `337sqrt(3)/16` 第一次出現在第 12 beat，完整數對第一次出現在第 13 beat。

## Beat map

### 01 meet_one_unit_triangle

只出現一個邊長 1 的正三角形。先建立固定物件與尺度，再問下一個從哪裡開始。

### 02 slide_the_second_half_side

複製同一個三角形，實際水平移動 `1/2`。位移箭頭落定後才填出相鄰交集。

### 03 add_the_third_and_trace

以相同動作加入第三個。三個原三角形淡化，外輪廓才以珊瑚色逐段描出。

### 04 measure_one_perimeter_increment

並排比較 `n=2` 與 `n=3`。舊末端斜邊長 1；新末端三段為
`1/2+1/2+1=2`，斜邊淨增 1。底邊另增 `1/2`，合計 `Delta r=3/2`。

### 05 generalize_the_perimeter

在一般示意條帶上分色數完斜邊與底邊，建立 `r_n=3(n+1)/2`。不代入 112。

### 06 measure_one_triangle_area

回到單一正三角形。畫高與半底，先求 `h=sqrt(3)/2`，再得到單一面積。

### 07 measure_one_adjacent_overlap

填出一個相鄰交集，標明三邊皆為 `1/2`；用相似縮放平方得到 `sqrt(3)/16`。

### 08 rule_out_hidden_triple_overlap

同時顯示三個三角形。標出第一與第三只共用一個點，說明非相鄰交集與三重交集
都沒有正面積，所以只扣 `n-1` 個相鄰交集。

### 09 audit_small_areas_and_generalize

逐一顯示 `n=1,2,3` 的精確聯集面積 `4、7、10` 個 `sqrt(3)/16` 單位，
再建立容斥式與 `s_n=(3n+1)sqrt(3)/16`。

### 10 hold_before_both_values

兩張卡分別放入 `r_112` 與 `s_112` 的代入式，但兩邊都只有問號。講者留真實
停頓，不說任何化簡結果。

### 11 reveal_the_perimeter

先描外輪廓，再化簡周長，第一次顯示 `r=339/2`。

### 12 reveal_the_area

把同一聯集填色，再化簡面積，第一次顯示 `s=337sqrt(3)/16`。

### 13 return_to_the_traced_union

輪廓與填色同時保留，分別標回 `r` 與 `s`，最後才顯示完整數對。

## Build and QA constraints

- 場景類別為 `CarloTcfs112MathQ10`；十三個 beat 與 TOML、講者稿、Slides
  manifest 同序，全部不循環。講者稿應有 12 個 `[NEXT]`、0 個 `[LOOP]`。
- 匯入期檢查必須驗證半格平移、完整兩兩與三重交集、小例聯集多邊形、一般式
  與 `n=112` 結果，且不能從畫面字串反讀答案。
- Python compile/import 與 beat parity 通過後，以隔離 media 目錄透過
  `manim-slides` wrapper 執行 1920x1080 高畫質渲染。
- 代表畫面至少涵蓋半格移動、第三個落點、輪廓描線、周長替換段、一般周長、
  高與單一面積、相鄰交集、單點三重邊界、小例容斥、雙問號停點、兩次結果揭示
  與最終回圖。
- 另以固定時間取樣檢查動畫中間態，特別防止移動中的三角形跳位、公式字形交叉、
  邊界描線短暫缺段、答案在 pause 前閃現、footer 或最終數對裁切。
