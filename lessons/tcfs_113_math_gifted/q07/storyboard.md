# 第 7 題 storyboard｜被二次函數保留下來的區間

## Teaching intent

這題真正需要理解的不是因式分解，而是「區間放在拋物線的哪一側，會決定最高值與最低值出現在哪裡」。課程因此保留同一張座標平面與同一個區間，讓區間依序移到頂點左側、跨過頂點、再落到右側。前兩個位置都利用畫面上可見的全域下界 `f(x) >= 3` 排除；這一步只比較高度，不先堆疊分類代數。

區間確定在遞增支後，才沿著兩個端點往曲線上連線。觀眾先看見左端對應最低高度 `f(a)`、右端對應最高高度 `f(b)`，接著才把題目給的名稱 `a`、`b` 對上去，得到兩條固定點方程式。最後加入 `y=x`，把「函數值等於輸入值」轉成兩條曲線的交點，再用因式分解算出 3、4。

結尾不以有序數對停住。區間回到 `[3,4]`，並直接由 `0 <= (x-3)^2 <= 1` 檢查整段值域確為 `[3,4]`。這讓最後答案重新連回開場的區間問題，而不是只驗兩個端點。

預估 8 分鐘。十個 beat 全部為非循環頁；每個停點都留下可慢慢指認的完整構圖。

## Source and provenance

- 典藏頁面：`https://sites.google.com/chjs.ntpc.edu.tw/carlovemath/%E4%B8%AD%E4%B8%80%E4%B8%AD%E8%B3%87%E5%84%AA%E7%8F%AD/113%E4%B8%AD%E4%B8%80%E4%B8%AD%E8%B3%87`。
- 題目來源：頁面嵌入的 `113中一中資優班解析.pdf`，Drive ID `1QQhuf8PqZMVyCdF9s9LHR8Q3y8RmAk3M`，SHA-256 `c4c5c0d834f7ef8f423d21265c6a69cadcaedfc5f2de290a79d0bc61b3844273`，第 7 頁填充第 7 題。
- 解題來源：正哥愛數學公開影片 `https://www.youtube.com/watch?v=xRrA7_xEStU`，provider ID `youtube:xRrA7_xEStU`，標題 `113中一中資優班填充7-8`，存取狀態 `public_stream`。
- 影片長度為 377.894 秒。逐段影格檢查顯示第 7 題從 00:00 到 03:31 都是穩定畫面；03:32 是頁面切換中的模糊影格，第 8 題從 03:33 起才穩定出現，所以本題定位記為 `00:00-03:31`。
- 來源將函數配方為 `(x-3)^2+3`，依區間相對於 `x=3` 的位置分成三類；在右側遞增情況使用 `f(a)=a`、`f(b)=b`，最後求得 `a=3`、`b=4`。本課保留這個數學骨架，但以原創座標圖、移動區間、端點高度與固定點交會重新表達，不使用來源 PDF 或影片的畫面、聲音、文字或圖像。
- 專案原創課程內容採 CC BY 4.0，程式碼採 MIT；來源素材不納入本專案授權。

## Independent reconstruction

題目給定正實數 `0<a<b`，並在 `[a,b]` 上考慮

\[
f(x)=x^2-6x+12=(x-3)^2+3.
\]

因此對所有實數 `x` 都有 `f(x)>=3`。先考慮 `a<3`：

1. 若 `b<3`，函數在 `[a,b]` 上遞減，最低值是 `f(b)>3>a`，不可能等於題目指定的 `a`。
2. 若 `b>=3`，區間包含 `x=3`，最低值是 `f(3)=3>a`，仍不可能等於 `a`。

兩種情況涵蓋所有 `a<3`，故必有 `a>=3`。此時 `[a,b]` 全在右側。若 `3<=x<y`，則

\[
f(y)-f(x)=(y-x)(x+y-6)>0,
\]

所以 `f` 在 `[a,b]` 上遞增。最低值與最大值分別為 `f(a)`、`f(b)`。題目又指定它們分別是 `a`、`b`，因此

\[
f(a)=a,\qquad f(b)=b.
\]

任一端點 `x` 都必須滿足

\[
x^2-6x+12=x
\iff x^2-7x+12=0
\iff (x-3)(x-4)=0.
\]

候選值只有 3、4。由 `a<b` 得 `(a,b)=(3,4)`。最後檢查：若 `3<=x<=4`，則 `0<=x-3<=1`，故

\[
0\le (x-3)^2\le1
\quad\Longrightarrow\quad
3\le f(x)\le4.
\]

而兩端值分別是 `f(3)=3`、`f(4)=4`，所以 `[3,4]` 上的實際值域正是 `[3,4]`，答案成立。

## Visual semantics

- 藍色：拋物線 `y=f(x)`。
- 紫色：目前被觀察的區間曲線，以及固定點直線 `y=x`。
- 黃色：左端點 `a` 與較低端點高度。
- 珊瑚色：右端點 `b` 與較高端點高度。
- 青綠色：最低高度 3、可行結論與最後值域。
- 灰色：座標軸、輔助線、已排除但仍需保留的脈絡。

## Beat plan

### 01 reveal_vertex_form

先畫座標軸與拋物線，再顯示 `y=(x-3)^2+3`。水平線與頂點共同建立全域下界 `f(x)>=3`。右側只列題目要求的區間和極值名稱，停在「區間可以放在哪裡？」

### 02 test_left_interval

把同一個 `[a,b]` 放在 `x=3` 左側，沿區間加亮下降的曲線。右端高度 `f(b)` 是最低值，卻仍大於 3，而 `a<3`；以 `f(b) != a` 排除。

### 03 test_straddling_interval

將區間平滑移動並拉長，使 `a<3<=b`。最低點 `(3,3)` 落在區間中，實際最低值為 3，但 `a<3`，再次不符。

### 04 settle_right_branch

區間移到右側。把前兩類 `a<3` 位置劃掉，留下 `3<=a<b`，再單獨顯示已經得到的必要條件 `a>=3`。

### 05 connect_endpoint_extrema

從 `a`、`b` 各自作垂直輔助線到曲線，顯示右支一路上升。先只命名實際最低值 `f(a)` 與實際最大值 `f(b)`，不寫固定點方程式。

### 06 earn_fixed_endpoint_equations

將圖上的實際極值名稱和題目指定的名稱並排。兩組名稱各自合併為 `f(a)=a`、`f(b)=b`，以物件連續性說明方程式從哪裡來。

### 07 locate_fixed_points

區間降彩度，加入紫色直線 `y=x`。兩個交點逐一亮起，但暫時不標數值；畫面只問哪些橫座標能使輸入與輸出相同。

### 08 solve_fixed_points

右側逐行寫出固定點方程式、移項與因式分解。最後得到候選集合 `{3,4}`，同時在左圖的兩個交點標上座標。

### 09 assign_ordered_endpoints

把候選值 3、4 分成兩個可移動籌碼。條件 `a<b` 先出現，再把 3 配給 `a`、4 配給 `b`，得到有序數對。

### 10 verify_preserved_interval

移除固定點直線，讓區間精確落在 `[3,4]`。加亮從 `(3,3)` 到 `(4,4)` 的整段拋物線，逐行建立平方範圍，得到 `3<=f(x)<=4`，最後確認最低值與最大值都符合原條件。

## Technical contract

- 場景類別為 `CarloTcfs113MathQ07(CarloSlide)`，透過 Manim Slides 產生十段非循環 segment。
- beat ID、`lesson.toml` 順序與講者稿標題必須完全一致；講者稿有九個 `[NEXT]`，沒有 `[LOOP]`。
- 繁體中文只使用專案 `label()` / `Text` 層；`MathTex` 字串僅含 ASCII LaTeX。
- 固定 16:9、1920x1080、30 fps。渲染必須使用專屬 `build/media/carlo_tcfs_113_math_gifted_q07`，避免平行工作共用 TeX 暫存。
- 只可編輯 `lessons/tcfs_113_math_gifted/q07/` 內的四個來源檔；collection、共享元件、測試與 QA attestation 由整合者處理。
