# 第壹部分第 11 題｜講者稿

解題來源：正哥愛數學
來源定位：115 學年度數學學科能力評量答案，第壹部分第 11 題，主要解答在 PDF 第 7 頁。
預計時間：約 11 分鐘。

## 01 build_cube｜截平面在每個面上留下什麼

先建立這個正立方體。

等一下會有一個平面斜斜穿過它。我們不需要一開始就想像完整截面，只要問一個比較小的問題：它穿過每個正方形面時，會留下什麼？

[PAUSE]

兩個平面相交，會留下一條直線。這就是我們追蹤截痕的工具。

[NEXT]

## 02 place_pqr｜三個點先固定平面

`P` 是 `EF` 的中點，所以兩邊都是一半。

`R` 是 `HD` 的中點，也把這條邊分成相等兩段。

`Q` 在 `EH` 上，並且 `EQ:QH=2:3`。

這三個不共線的點決定唯一一個平面。現在我們只追它在立方體各面留下的線。

[NEXT]

## 03 trace_known_section｜同一面上有兩點，就有一條線

`P` 和 `Q` 都在上表面，所以截平面在上表面的交痕，一定沿著直線 `PQ`。

`Q` 和 `R` 都在後表面，所以那一面的交痕沿著 `QR`。

每找到同一個面上的兩個點，就能把截線繼續傳到下一個面。

[NEXT]

## 04 extend_top_line｜先在上表面找兩個橋接點

延長上表面的 `PQ`。

往 `FG` 這一側，它和 `FG` 的延長線交於 `L`。往另一側，它和 `GH` 的延長線交於 `I`。

`L` 和 `I` 雖然落在正方形外，仍同時位於上表面所在平面與我們的截平面中。

請記住共線順序：`L、P、Q、I`。

[NEXT]

## 05 extend_right_line｜把截線送到右表面

現在只看右表面 `CDHG`。

`I` 位於 `GH` 的延長線，`R` 位於 `HD`，所以兩點都在這個面的延伸平面中。

連接並延長 `IR`。它交 `CD` 於題目中的 `M`，也交 `CG` 的延長線於 `J`。

後面找 `N` 時，需要的橋接點就是 `J`。

[NEXT]

## 06 locate_n｜前表面的兩個共同點

轉到前表面 `BCGF`。

`L` 在 `FG` 的延長線上，`J` 在 `CG` 的延長線上，所以兩點都位於前表面所在平面。

另一方面，它們也都是我們沿著截平面延伸得到的點。因此連線 `LJ` 正是截平面在前表面的交痕。

`LJ` 與 `FB` 的交點，就是 `N`。

我們已經畫出 `N`；接著才計算它把 `FB` 分成什麼比例。

[NEXT]

## 07 unfold_three_faces｜把空間問題攤平成三張圖

比例計算不需要在透視圖上硬看。

把上表面、右表面和前表面依序攤平。所有點都跟著原來的面移動，立方體邊長統一記作 `a`。

接下來，長度會從上表面傳到右表面，再傳到前表面的 `FN`。

[NEXT]

## 08 top_left_ratio｜先在上表面量到 LF

看上表面左側的兩個小直角三角形 `PEQ` 與 `PFL`。

它們有一對對頂角，也各有一個直角，所以相似。

而 `P` 是 `EF` 中點，`PE=PF=a/2`。相似比等於 1，因此對應的另兩邊也相等。

所以 `LF=EQ=2a/5`。

[NEXT]

## 09 top_right_ratio｜同一條線量到 I 的位置

再看 `PEQ` 與右側的 `IHQ`。

已知 `EQ:QH=2:3`，相似三角形同時給出 `PE:HI=2:3`。

`PE=a/2`，所以 `HI=3a/4`。

從 `I` 到 `G` 還要再走一整條 `HG=a`，因此 `IG=3a/4+a=7a/4`。

[NEXT]

## 10 right_face_ratio｜把長度傳到 GJ

現在進入右表面。

三角形 `IHR` 與 `IGJ` 由同一條斜線和兩條平行直立邊形成，所以相似。

`R` 是 `HD` 中點，因此 `HR=a/2`。

比例是 `HI/IG=HR/GJ`。代入 `3a/4`、`7a/4` 與 `a/2`，得到 `GJ=7a/6`。

這段 `GJ` 會成為下一個面的已知長度。

[NEXT]

## 11 front_face_ratio｜最後傳到 FN

前表面中，三角形 `LFN` 與 `LGJ` 相似。

先把整段 `LG` 算出來。它是 `LF+FG=2a/5+a=7a/5`。

因此 `LF/LG=FN/GJ`。

把 `LF=2a/5`、`LG=7a/5`、`GJ=7a/6` 放入，得到 `FN=a/3`。

[NEXT]

## 12 finish_ratio｜把整條 FB 分成兩段

整條 `FB` 長為 `a`，上段 `FN` 已知是 `a/3`。

所以剩下的 `NB=a-a/3=2a/3`。

兩段的比例是 `(a/3):(2a/3)=1:2`。

因此 `FN/NB=1/2`。

[NEXT]

## 13 consolidate｜一條截線跨過三個面

最後把空間路徑再走一次。

上表面延長 `PQ` 找到 `L` 與 `I`；右表面延長 `IR` 找到 `J`；前表面連接 `LJ`，才找到 `N`。

比例也沿同一方向傳遞：上表面量到 `LF` 與 `IG`，右表面量到 `GJ`，前表面量到 `FN=a/3`。

所以 `N` 把 `FB` 分成 1 比 2，答案是 `1/2`。

[PAUSE]
